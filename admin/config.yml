backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "propiedades"
    label: "Propiedades"
    folder: "data/propiedades"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Título", name: "titulo", widget: "string" }
      - { label: "Precio", name: "precio", widget: "number" }
      - { label: "Ciudad", name: "ciudad", widget: "string" }
      - { label: "Características", name: "features", widget: "text" }
      - { label: "Imagen principal", name: "imagen", widget: "image" }
